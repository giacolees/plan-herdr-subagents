# Decisions

Permanent architectural decisions and their short rationale.

Record a decision only when it is hard to reverse, surprising without context,
and the result of a real trade-off. Use `docs/adr/` for a full ADR when warranted.
